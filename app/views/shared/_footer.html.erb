
<div class="d-flex justify-content-evenly text-dark p-2">   
    <% if current_user.present? %>
        <%#= button_to "", destroy_user_session_path, method: :delete, 
            data: {turbo: "false"},
            class: "fa-solid fa-right-from-bracket btn btn-danger me-2"  %>
        <%#= current_user.email %>

        <%#= link_to root_path, class: "btn btn-link  
            #{' orange-colored' if controller_name == 'pages' && action_name == 'index'}" do %>
            <!--i class="fa fa-home"></i>
            <div class="">Let's Shado!</div-->
        <%# end %>

        <%= active_link_colored(root_path, "Let's Shado!", "home", 'pages', 'index') %>

        <%# adapter propremenr pour type user %>
        <% if athlete_user %>   
            <%= active_link_colored(athletes_path, "Profile", "user", 'athletes', 'index') %>
            <%= active_link_colored(dashboard_athlete_activities_path, "Dashboard", "dashboard", 'dashboard_athlete', nil) %>
        <% end %>

        
        <%= active_link_colored(root_path, "Chat", "message", 'messages', 'index') %>

    <% else %>
    
        <%= link_to '', new_user_session_path, class: "fa-solid fa-user-lock btn btn-warning mx-2" %>
        <%= link_to '', new_user_registration_path, class: "fa-solid fa-user-plus btn btn-primary" %>
    <% end %>  
</div>
